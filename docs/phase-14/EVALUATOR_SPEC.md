# Phase 14 Evaluator Specification

The evaluator executes the real subject-pack registry, authorization, namespace, renderer-capability, client-state, scoring-parity, account-sync, and expansion-scorecard code. It verifies that Paper 2 cannot use Paper 1 authorization; subject data and learner state remain isolated; authorized Paper 2 can start only with a matching current fingerprint; unsupported renderer capabilities fail before session creation; web/iOS/Android derive identical state and scores from the same events; onboarding-name differences resolve to one authenticated account; stale client versions are rejected; unhealthy Paper 1 produces kill or hold; healthy measured inputs can produce scale; and unapproved adjacent exams remain disabled. Green software does not claim a real Paper 2 corpus or deployed native clients.